import type { Config } from '@repo/payload-types'

// Placeholder: shared Payload types are available for SDK queries in later work.
type _PayloadConfig = Config

export default function HomePage() {
  const _configCheck: _PayloadConfig | null = null
  void _configCheck

  return (
    <main>
      <h1>Journeys</h1>
      <p>Travel blog — coming soon.</p>
    </main>
  )
}
