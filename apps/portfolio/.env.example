# Database connection string
DATABASE_URL=mongodb://127.0.0.1/portfolio

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Optional fallback/canonical production URLs on Railway (comma-separated)
# First valid URL is treated as canonical if NEXT_PUBLIC_SERVER_URL is unset.
RAILWAY_PROJECT_PRODUCTION_URLS=https://www.example.com,https://example.com

# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE

# Seed admin user (optional, for pnpm seed)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme

# Cloudflare R2 (optional — omit to use local public/media storage)
R2_BUCKET=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://media.example.com

# Resend email adapter (required for auth emails like password reset)
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@example.com
RESEND_FROM_NAME=Portfolio

# Umami Cloud analytics (optional)
NEXT_PUBLIC_UMAMI_SRC=https://cloud.umami.is/script.js
NEXT_PUBLIC_UMAMI_WEBSITE_ID_PORTFOLIO=
NEXT_PUBLIC_UMAMI_WEBSITE_ID_JOURNEYS=

# Railway mapping notes:
# - DATABASE_URL: reference from your Railway MongoDB service connection URL.
# - For Railway Object Storage, map generated AWS-style vars to these names:
#   - R2_BUCKET <- AWS_S3_BUCKET_NAME
#   - R2_ACCESS_KEY_ID <- AWS_ACCESS_KEY_ID
#   - R2_SECRET_ACCESS_KEY <- AWS_SECRET_ACCESS_KEY
#   - R2_ENDPOINT <- AWS_ENDPOINT_URL
# - R2_PUBLIC_URL is optional: use your public bucket/custom CDN URL if configured.
