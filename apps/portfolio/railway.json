{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable pnpm && pnpm install --frozen-lockfile && pnpm turbo build --filter=portfolio"
  },
  "deploy": {
    "startCommand": "corepack enable pnpm && pnpm --filter portfolio start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
